Commit message

Add create action to orders controller.
Add method to order model to remove all items from cart and add to the order.
Make associations between LineItems and Carts/Orders optional, because you can't create a LineItem without these assocations present otherwise.
Fix Shopping Cart display in navbar, it still doesn't display numbers.
